# 个人工作台（生活·学习全能台）实现方案

> 调研 + 设计文档。本文档只做方案，不写业务代码。请你确认后方可进入编码阶段。
> 生成日期：2026-08-14

---

## 一、GitHub 开源项目调研对比

### 1.1 自托管仪表盘类（Homelab Dashboard）

| 项目 | 技术栈 | 配置方式 | 数据存储 | 集成/模块 | 优点 | 缺点 | Stars(约) |
|---|---|---|---|---|---|---|---|
| **Homepage** (gethomepage/homepage) | Next.js (SSG) | YAML 多文件 | 无(静态) | 100+ 服务 widget、Docker 发现、Prometheus 等 | 集成最深、资源占用低(~50MB)、可 git 追踪 | 无可视化编辑器、学习曲线陡 | 28k+ |
| **Homarr** (homarr-labs/homarr) | Next.js / TS | 拖拽 GUI | SQLite | 服务集成、多 board、用户权限 | 点点点即可配、最易上手、移动适配 | 资源占用高(~300MB)、v1 重写旧配置不兼容 | 3k+ |
| **Dashy** (Lissy93/dashy) | Vue.js | YAML + GUI 编辑器 | YAML 文件 | 50+ widget、50+ 主题、认证、PWA | 可定制性最强、主题丰富、导出导入 | 配置即代码门槛、资源中等(~80MB) | 24k+ |
| **Homer** | 纯静态 YAML | 单 YAML | 无 | 书签页 | 极轻(~10MB)、零后端 | 不能编辑、无集成、定制受限 | 11k+ |
| **Heimdall** | PHP / Laravel | GUI | SQLite | 增强型 app 卡片 | 新手友好、有用户系统 | 最重(~150MB)、布局固定 | 9k+ |
| **Flame** | Node.js + SQLite | GUI + Docker 标签 | SQLite | 书签、天气、Docker 自动发现 | 轻量(~15MB)、快 | 功能有限、无服务集成 | 6k+ |
| **Glance** (glanceapp/glance) | Go 单二进制 | 单 YAML | 无 | RSS/YouTube/Reddit 聚合 feed | 最轻(~20MB)、开箱极美、加载快 | 偏信息流、非任务型 | 31k+ |
| **Lab Dash** | React + Node | GUI | 文件(可选加密) | 拖拽 widget、PWA、Docker | PWA 可装桌面、本地加密 | 非完整监控、需 Docker 基础 | 559 |

**小结**：这类项目普遍是「服务端 + 容器」架构，面向家庭实验室导航，并不适合「纯浏览器、离线、零运维」的个人生活工作台。但其**模块化卡片、拖拽排布、PWA、主题系统、导出导入**的思路值得借鉴。

### 1.2 生活 / 学习管理类

| 项目 | 技术栈 | 架构 | 模块 | 优点 | 缺点 |
|---|---|---|---|---|---|
| **Personal Management System** (Volmarg/personal-management-system) | Symfony / PHP + 前端分离 | 后端 API + 前端 | 任务/目标、笔记、联系人、加密密码、财务、日程、文件 | 模块化清晰、可扩展、自托管隐私好 | 后端依赖重(PHP/DB)、前端需另配、社区维护有限 |
| **LifeBoard** (zxc7563598/life-board) | PHP Webman + Vue3 + MySQL/Redis | 前后端分离 | 财务(邮件解析账单)、任务(日历视图)、快捷搜索、热榜；开发中：打卡/情绪/日记/健康 | 理念好(Notion+Obsidian+Todoist 综合体)、数据自有 | 部署重(PHP8.1/MySQL/Redis)、imap 解析账单偏复杂 |
| **Talisman** (eustrainLee/talisman) | Electron + Vite + TS | 桌面应用 | 文档(MD)、资产/记账、任务(多级队列)、认知梳理 | 「工具是用的、不反客为主」理念好、桌面端 | 非 Web、需安装、模块大多未完工 |
| **Habitus** | Go + SQLite | 单二进制服务端 | 习惯创建/打卡/图表 | 极简、单文件部署、数据主权 | 仅习惯追踪、功能单一 |

**小结**：真正覆盖「生活+学习」的开源项目很少，且几乎都走上「服务端 + 数据库」重架构。对个人单机使用而言过重。核心可借鉴点：**任务/目标进度、习惯追踪热力图、财务收支可视化、笔记灵感沉淀、模块化扩展**。

### 1.3 UI 简洁漂亮参考（用于提炼视觉规范）

| 项目 | 技术栈 | 设计亮点 |
|---|---|---|
| **Tabler** | Bootstrap 5 | 简洁优雅、暗色模式、组件丰富、留白克制 |
| **Volt React Dashboard** | Bootstrap 5 + React | 100+ 组件、数据驱动、卡片化 |
| **Vue Soft UI Dashboard** | Vue3 + Bootstrap5 | Soft UI 柔和设计、圆角柔和阴影 |
| **Nange / Dmego Home / Tomotoes** | 原生 HTML/CSS | 个人主页极简、动态背景、零依赖 |
| **damesck** | React + TS | 毛玻璃(iOS 风)、流畅动画、响应式 |
| **Onur Dev** | React + Tailwind | 极简、模块布局实用 |

**UI 可借鉴点**：卡片化 + 圆角 + 柔和阴影 + 充足留白；CSS 变量驱动主题(浅/暗)；线性 SVG 图标(Tabler 风格)；系统字体栈；色板克制(主色 + 中性灰)。

---

## 二、关键取舍：为什么不用那些框架

| 维度 | 开源大项目做法 | 本工作台做法（遵循小台规范） |
|---|---|---|
| 部署 | Docker / 服务器 / 数据库 | 单文件 HTML，双击即用，离线可用 |
| 依赖 | Next/Vue/Go/PHP + CDN/库 | 全内联，零外部依赖，永不 404 |
| 数据 | MySQL/SQLite/服务端 | localStorage 本地存储 + JSON 导出备份 |
| 图表 | Chart.js / ECharts | 手写内联 SVG（进度环/柱状/饼/热力图） |
| 图标 | Font Awesome / 图标包 | 内联 SVG path，禁用 emoji |
| 适配 | 响应式但偏桌面 | PC 侧栏 + 移动端底部 Tab，单列，按钮≥44px |

**结论**：大型开源项目提供「模块划分与功能清单」的灵感，但架构上我们采用小台工作台规范（单文件 + 本地存储 + 内联 SVG），更契合「个人、离线、零运维、可部署到手机桌面」的目标。

---

## 三、我们的架构设计

**技术栈**：纯 HTML + 内联 CSS + 内联 JS（ES6），零外部依赖。
**存储层**：`localStorage`，key 前缀 `wb_life_`；封装 `load/save/exportJSON/importJSON/clear(二次确认)`。
**图表**：手写 SVG 函数库（环形进度、柱状、饼图、周/月热力图）。
**图标**：内联 SVG path 集合（线性风格）。
**布局**：
- PC：左侧/顶部导航 + 主区卡片网格
- 移动：底部 Tab 导航 + 单列卡片，适配安全区 `env(safe-area-inset-bottom)`
**首屏**：顶部「今天要处理」置顶区（逾期红标 + 一键处理；昨日未完成自动顺延）+ 导出/导入/清空按钮。
**预置数据**：首次打开 3–5 条示例（含 1 条逾期），附「清空示例」按钮。

### 模块方案（标签分页，避免单屏过载）

采用**底部/侧边 Tab 分模块**，每个 Tab 是一个功能模块——既满足「尽量覆盖生活学习」又不违反「单屏不过载」原则。建议首批做 4 个核心模块：

1. **仪表盘 / 今日要处理**（聚合页）：逾期/今日/临近条目汇总、各模块概览卡片、快捷入口。
2. **任务待办**：优先级 P0/P1/P2、完成切换、截止日、逾期标红、顺延。
3. **习惯打卡**：多习惯矩阵、连续天数、周/月热力图(SVG)。
4. **记账理财**：收支录入、分类标签、月预算进度环、支出饼图（红支绿收、¥）。

**后续可迭代追加（每次 1 个）**：学习/备考看板（倒计时+科目进度+错题本）、健康减脂（体重折线）、笔记灵感、日程日历、目标拆解。

---

## 四、UI 设计方向（综合各家）

- **设计语言**：简洁卡片 + 大圆角 + 柔和阴影 + 充足留白（参考 Tabler / Soft UI 的克制感）。
- **主题**：浅色为主，CSS 变量驱动，预留暗色切换位。主色建议「青蓝/靛蓝」系（稳重、干练），中性灰底。
- **图标**：线性 SVG（Tabler 风格），统一 24×24 viewBox。
- **图表**：手写 SVG，配色与主题一致，动效克制（hover 微交互）。
- **响应式**：栅格自适应，移动端单列、按钮 ≥44px、输入框 ≥16px、数字用 `type=number`。

---

## 五、实现计划（分阶段，确认后执行）

- **Phase 0 脚手架**：单文件骨架、主题 CSS 变量、响应式框架、Tab 导航、存储层封装、示例数据机制。
- **Phase 1 核心模块**：仪表盘(今日要处理) + 任务 + 习惯 + 记账 四个模块跑通。
- **Phase 2 数据闭环**：导出 JSON / 导入恢复(不限条数) / 清空二次确认 / 30 条备份提示。
- **Phase 3 图表与视觉打磨**：SVG 进度环、饼图、热力图；移动端细节；空状态。
- **Phase 4 部署上线（可选）**：PWA manifest 内联 + 静态托管，教「手机添加到主屏幕」。

---

## 六、待你确认的问题

1. **模块范围**：是否采用「Tab 分模块」方案？首批就做上述 4 个（仪表盘/任务/习惯/记账），还是调整？
2. **覆盖广度 vs 精简**：是否现在就把「学习备考/健康减脂/笔记」也纳入首批，还是先 4 个跑通再迭代？
3. **是否需要部署上线**（手机当 APP 用）？还是先本地 HTML 文件？
4. **配色偏好**：默认青蓝/靛蓝稳重风，还是你有其他偏好（如暖色、暗色优先）？

> 你确认以上任一项后，我再开始编写代码。
